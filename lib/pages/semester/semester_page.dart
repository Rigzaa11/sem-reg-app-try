import 'package:flutter/material.dart';

class SemesterPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Implement the UI for the Semester Page here.
    return Container(
      child: Center(
        child: Text('Semester Page'),
      ),
    );
  }
}
