import 'package:flutter/material.dart';

class PersonalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Implement the UI for the Personal Page here.
    return Container(
      child: Center(
        child: Text('Personal Page'),
      ),
    );
  }
}
