import 'package:flutter/material.dart';

class FinancialPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Implement the UI for the Finacial Page here.
    return Container(
      child: Center(
        child: Text('Financial Page'),
      ),
    );
  }
}
